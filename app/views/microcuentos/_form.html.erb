<%= form_for(@microcuento) do |f| %>
  <% if @microcuento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@microcuento.errors.count, "error") %> prohibited this microcuento from being saved:</h2>

      <ul>
      <% @microcuento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Título' %><br />
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label 'Contenido' %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Publicar",:class=>"btn btn-primary"%>
  </div>
<% end %>
