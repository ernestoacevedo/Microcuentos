<%= form_for(@microcuento, :html => { :multipart => true }) do |f| %>
  <% if @microcuento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@microcuento.errors.count, "error") %> prohibited this microcuento from being saved:</h2>

      <ul>
      <% @microcuento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="span4">
      <div class="field">
        <%= f.label 'Contenido' %><br />
        <%= f.text_area :content, :class=>"escribir" %>
      </div>
    </div>

    <div class="span4">
      <div class="field">
        <%= f.label 'Título' %><br />
        <%= f.text_field :title %>
      </div>

      <div class="field">
        <%= f.label 'Portada' %><br />
        <div class="fileupload fileupload-new" data-provides="fileupload">
        <div class="fileupload-preview thumbnail" style="width: 300px; height: 200px;"></div>
        <div>
          <span class="btn btn-file"><span class="fileupload-new">Seleccionar imagen</span><span class="fileupload-exists">Cambiar</span><%= f.file_field :portada %></span>
          <a href="#" class="btn fileupload-exists" data-dismiss="fileupload">Eliminar</a>
        </div>
        </div>
      </div>  
    </div>

  </div>
  <%= f.submit "Publicar",:class=>"btn btn-primary btn-block"%>

  

  
<% end %>
